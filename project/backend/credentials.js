var user 
var host 
var password 
var port 
var database 
var dbUri
var path

user = '';
host = 'localhost';
password = '';
port = '5432';
database = 'oose';
path = '';

export  {
    user as user,
    host as host,
    password as password,
    port as port,
    database as database,
    path as path,
}

