const user = 'postgres'
const host = 'localhost'
const password = 'postgres'
const port = '5432'
const database = 'oose'
export  {
    user as user,
    host as host,
    password as password,
    port as port,
    database as database,
}

